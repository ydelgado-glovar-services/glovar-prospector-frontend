/**
 * app/login/layout.tsx — Login layout.
 *
 * Minimal layout for the login page.
 * No AppHeader or navigation — clean, focused auth experience.
 */

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <>{children}</>
}
